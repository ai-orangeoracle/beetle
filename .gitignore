# Rust
/target/
Cargo.lock
beetle.cdx.json
bom.json

# ESP-IDF / embuild
.embuild/

dev/
# Internal design notes / migration drafts (not part of public docs)
dev-docs/

# 外置配置前端（configure-ui）
configure-ui/node_modules/
configure-ui/dist/

# IDE / OS
.idea/
.vscode/
.claude/
.DS_Store
*.swp
*.swo


# 构建产物（可选，按需取消注释）
# *.bin
# spiffs.bin

# intent-trainer
intent-trainer/.venv/
intent-trainer/model/

# Optional bundled WiFi binaries (large; may ship via release tarball only)
packaging/linux/embed-deps/armv7/*
!packaging/linux/embed-deps/armv7/.gitkeep
packaging/linux/embed-deps/aarch64/*
!packaging/linux/embed-deps/aarch64/.gitkeep
packaging/linux/embed-deps/x86_64/*
!packaging/linux/embed-deps/x86_64/.gitkeep
