#!/usr/bin/env bash
# Runs the same §14.2 check as CI. Enable once per clone: git config core.hooksPath .githooks
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec bash "$ROOT/scripts/check_platform_isolation.sh"
