---
description: Luckfox SDK 本地修改后同步到 Docker 编译容器
globs: linux/Luckfox_Lyra_SDK_250815/**/*
alwaysApply: false
---

# Luckfox SDK 与 Docker 编译环境

## 前提

- 全量编译在容器 **`ubuntu2204`** 内进行，`SDK` 根目录挂载/复制为 **`/work`**（`luckfox-sdk-work` 数据卷）。
- 仓库里的 **`linux/Luckfox_Lyra_SDK_250815/`** 与容器 **`/work`** 是**两份树**；只改本地文件**不会**自动进容器。

## 必须遵守

凡编辑、新增、删除 **`linux/Luckfox_Lyra_SDK_250815/`** 下任意文件（含 `buildroot/configs/`、`device/`、`check-sdk.sh` 等），在让用户去容器里 **`./build.sh`** 之前，须：

1. 运行同步脚本（推荐）：
   ```bash
   ./scripts/sync-luckfox-sdk-to-docker.sh
   ```
2. 勿用裸 **`docker cp` 整树 SDK**：`.repo` 大包与部分符号链接会导致失败；脚本用 **临时 Alpine 容器 + `rsync`** 写入数据卷 **`luckfox-sdk-work`**（与 **`ubuntu2204`** 的 **`/work`** 为同一卷）。

## 脚本说明

- **`scripts/sync-luckfox-sdk-to-docker.sh`**：本地 **`linux/Luckfox_Lyra_SDK_250815/`** → 卷 **`luckfox-sdk-work`** 的 **`/work`**；**`ubuntu2204`** 启动后看到的内容即最新。
- 环境变量（可选）：`LUCKFOX_SDK_PATH`、`LUCKFOX_DOCKER_VOLUME`（默认 `luckfox-sdk-work`）、`LUCKFOX_DOCKER_CONTAINER`（仅用于 `docker start`，默认 `ubuntu2204`）。

## 注意

- 全量同步体积大时可能耗时数分钟，属正常。
- 改完 **`rockchip_*_defconfig`** 后若容器里已有 **`buildroot/output/.../.config`**，必要时需 **`./build.sh lunch`** 或按 Buildroot 流程重新合并配置后再编。
